//build api endpoint to return price for
    // a particular coin
    // on a particular date
    // based on a particular number

//build api endpoint to return price for
    // a particular coin
    // on todays date
    // based on a particular number

// If number A is greater than number B. Then change colour to green and calculate % growth
// If number A is less than number B. Then change colour to red and calculate % loss
